import React from "react";

import "./style.css";

const NotFound = () => (
  <div id="not-found-wrapper">
    <div id="not-found">
      <h1>Hmm... We can’t seem find the page you’re looking for.</h1>
    </div>
  </div>
);

export default NotFound;
